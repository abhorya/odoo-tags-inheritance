<odoo>
    <!-- Inherit Form View -->
    <record id="view_product_template_form_inherit" model="ir.ui.view">
        <field name="name">product.template.form.inherit</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='group_general']" position="inside">
                <field name="company_tag"/>
            </xpath>
        </field>
    </record>

    <!-- Inherit Tree View -->
    <record id="view_product_template_tree_inherit" model="ir.ui.view">
        <field name="name">product.template.tree.inherit</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="company_tag"/>
            </field>
        </field>
    </record>

    <!-- Product Search View -->
    <record id="view_product_template_search_inherit" model="ir.ui.view">
        <field name="name">product.template.search.inherit</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="company_tag"/>
            </field>
            <search position="inside">
                <separator/>
                <filter string="My Product Tags" name="my_product_tags" 
                        domain="[('has_my_product_tag', '=', True)]"/>
                <group expand="0" string="Group By">
                    <filter string="Company Tag" name="group_company_tag" 
                            context="{'group_by': 'company_tag'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
